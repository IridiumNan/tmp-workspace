# tmp-workspace 终极完整版开发计划（暑假正式开发版）

## 0. 项目终极定位（范式革命）

### 传统开发者痛点（原生文件系统的结构性缺陷）

1. **目录规划前置焦虑**

新建临时脚本、验证想法必须提前选路径、建文件夹，创作前产生大量无意义心智负担。

1. **树形目录表达力不足**

传统文件夹是「单归属树形结构」，一个项目无法同时属于多个分类，无法表达多维属性。

1. **临时项目无专属悬置缓冲区**

代码刚写时定位模糊、价值未知，既不该放正式工程目录，也不该被系统临时目录清理，**没有“先创作、后定性”的中间态**。

1. **物理路径与逻辑状态强耦合**

归档、归类、切换状态需要 `mv` 移动实体文件，IO重、易损坏、无法原子化、无法批量操作。

1. **项目间依赖复用成本极高**

公共SDK、常用目录每次新建工程需要手动软链接，无法全局复用、批量挂载。

1. **无法量化项目价值与活跃度**

所有项目同质化，无法自动区分高频在用、低频闲置、即将废弃的工作区。

1. **多设备迁移、备份、同步极其繁琐**

### 本项目提出的全新开发范式

**彻底终结「用物理文件夹管理项目逻辑」的二十年旧范式**

1. **物理存储全权托管，用户彻底脱离路径管理**

2. **所有项目以独立 Workspace 为唯一顶层逻辑单元，完全平级**

3. **状态、分类、依赖、价值全部外挂结构化属性，不依赖物理位置**

4. **先创作、后定性、渐进式归档，适配人类真实研发思考流程**

5. **网状标签替代树形目录做顶层分类，多维交叉、可迭代、可批量**

6. **唯一数据本体 + 全局软链接状态层，实现原子状态切换、无损迁移、增量同步**

一句话总结：

**让开发者只关心“我要写一个新想法”，系统自动接管存放、归类、依赖、生命周期、价值评估、备份同步所有底层琐事。**

---

## 1. 全新终极架构（最终定稿，不再改动）

### 1.1 三层物理存储架构（核心重构：数据本体与状态解耦）

完全遵循 XDG 规范

```Plain Text

# 唯一真实数据源（所有文件本体永久存放这里）
~/.local/share/tmp-workspace/data/{12id}/

# 状态软链接层（仅链接，无实体文件）
~/.local/state/tmp-workspace/pending/     # 悬置待评估临时工作区
~/.local/state/tmp-workspace/archive/    # 已定型归档工作区

# 别名软链接层（用户可读别名，方便手动浏览）
~/.local/state/tmp-workspace/by-name/
```

#### 核心机制

1. **data/**：唯一真实本体，永不移动、永不复制

2. **pending/archive**：仅软链接，切换状态**只增删链接，不移动文件**，瞬时、原子、零IO风险

3. 所有失效、死链、状态不一致可一键重建

#### 生命周期新逻辑

- `new`：创建 data 本体 → pending 生成软链接

- `save`：删除 pending 链接 → archive 新建链接（本体不动）

- 过期清理：删除 pending 链接 → 本体进入GC宽限期 → 后台懒回收

### 1.2 四大核心模块（完整系统结构）

1. **工作区生命周期核心模块**

懒触发GC、状态机、防误删、原子写入、生命周期管理

1. **Dependency 全局依赖模块（全新拆分）**

全局可复用依赖库、系统路径/跨工作区引用、新建项目自动批量挂载

1. **Tag 网状分类模块**

替代顶层树形目录，多维交叉归类、批量筛选、批量操作

1. **智能价值评估模块**

多维度加权打分、终端视觉分层、智能预测跳转

### 1.3 配套工具生态

- `export` / `import` 一键备份还原

- 增量跨设备同步

- 全局文件系统自检、死链修复、元数据修复

---

## 2. 核心功能详细规范（最终版）

### 2.1 工作区基础核心（MVP 底层）

#### 核心特性

1. 12位十六进制全局唯一ID，支持前缀模糊匹配

2. 工作区完全平级，无任何父子目录嵌套

3. 懒清理机制：无常驻进程、无定时轮询

4. 活跃工作区自动续期、宽限期保护、防误删

5. 所有元数据、日志**原子写入**，断电不损坏

#### 基础命令

```Plain Text

tmp new [name]
tmp cd [id/name]
tmp ls
tmp del [id/name]
tmp save [path]
tmp clean
tmp log
tmp config
```

### 2.2 Dependency 独立依赖模块（重点新增）

解决：重复软链接、公共依赖无法复用、跨项目引用困难

#### 依赖两类类型

1. `system-path`：系统公共目录（Downloads、tmp、bin）

2. `workspace-ref`：引用其他已存在工作区（公共SDK、通用工具库）

#### 配置范式

```TOML

[dependency]
auto_mount = ["sys-tmp", "downloads", "go-sdk"]
prevent_cyclic_ref = true

[dependency.lib.sys-tmp]
type = "system-path"
target = "~/tmp"

[dependency.lib.go-sdk]
type = "workspace-ref"
target = "sdk-go-common"
mount_name = "vendor/sdk"
```

#### 依赖管理命令

```Plain Text

tmp dep list
tmp dep add
tmp dep rm
tmp dep info
tmp link [ws] --dep xxx
```

#### 能力

- 新建工作区自动批量挂载预设依赖

- 支持自定义挂载目录

- 循环依赖检测阻断

- 存量工作区可手动刷新依赖链接

### 2.3 Tag 网状多维分类系统

**彻底取代顶层树形文件夹管理**

- 一个工作区可绑定无限标签

- 一个标签可归类无限工作区

- 支持维度：语言、优先级、项目类型、团队/个人、临时/正式

#### 标签命令

```Plain Text

tmp tag add
tmp tag rm
tmp tag clear
tmp tag list

tmp ls --tag xxx
```

#### 批量能力

- 按标签批量归档、批量删除、批量导出

- 标签绑定分数权重，自动区分项目价值

### 2.4 智能价值打分 + 智能预测 CD

#### 打分维度（可配置权重）

- 新鲜度衰减

- 访问活跃度

- 文件变更产出量

- 归档标记加分

- 高优先级标签加分

- 临时实验标签扣分

#### 视觉分层展示

终端色彩 + 星级梯度，一眼识别项目价值层级

#### 智能预测

- 无参 `tmp cd` 自动进入**当前最高价值、最高概率**工作区

- 结合时序、标签、活跃度多维预测

### 2.5 Export / Import 一键迁移 & 增量同步

#### 核心优势（架构带来的天然能力）

- 仅同步 data 本体 + 元数据

- 所有状态链接、依赖挂载、标签配置**目标机器本地自动重建**

#### 命令

```Plain Text

tmp export --all / --archive / --tag xxx / --incremental
tmp import xxx.zst
```

#### 支持

- 全量备份

- 按状态/标签筛选导出

- 时间增量导出

- 跨设备无缝复刻完整开发环境

### 2.6 文件系统自检修复

```Plain Text

tmp fs-check      # 检查死链、元数据不一致
tmp fs-rebuild    # 一键重建所有软链接体系
```

---

## 3. 完整迭代开发 roadmap（暑假分阶段开发）

### P0 绝对核心底层（必做，骨架闭环）

1. XDG 三层目录架构实现

2. 工作区ID生成、创建、删除、基础切换

3. 原子写入元数据、事件日志系统

4. 懒触发自动清理机制

5. pending/archive 软链接状态切换逻辑

### P1 依赖模块重构（核心增量）

1. 废弃旧 `linked_dirs`，全新 `dependency` 模块

2. 配置解析、依赖注册、两类依赖支持

3. new 时自动批量挂载依赖

4. 循环依赖检测、冲突检测

5. dep、link 全套命令

### P2 Tag 标签系统 + 批量操作

1. 元数据 tags 字段扩展

2. 全套 tag 增删查命令

3. ls --tag 筛选

4. 按标签批量 save / clean / export

### P3 智能打分系统 + 视觉层级 + 智能CD

1. 多维度加权评分算法

2. 终端色彩、星级可视化

3. 标签分数加权配置

4. 无参智能预测跳转

### P4 导出导入 & 跨设备同步

1. export 全量/筛选打包

2. import 自动重建完整环境

3. 增量导出实现

4. 依赖配置跨设备同步

### P5 容错、自检、修复体系

1. fs-check 一致性检测

2. 一键重建所有链接

3. 旧版本平滑迁移逻辑

4. 死链自动清理

### P6 最终体验优化

1. 完整帮助文档、命令补全

2. 配置项全部可自定义权重

3. 终端视觉主题优化

4. 性能极致优化（懒计算、无多余遍历）

---

## 4. 本项目最终解决的「高层次问题」

1. **彻底消灭目录规划焦虑**

不用思考存放路径、不用提前分类、不用纠结归属

1. **适配人类「先试错、后定型」的开发流程**

pending 悬置池承接所有模糊期、草稿期项目

1. **网状标签取代落后的树形目录**

从「单路径强制归属」升级为「多维网状逻辑关联」

1. **依赖全局复用，彻底消灭重复软链接劳动**

2. **项目价值自动量化，无需人工整理归类**

3. **跨设备开发环境一键复刻、增量同步**

4. **物理与逻辑彻底解耦，批量操作、自动化能力拉满**

---

## 5. 为什么这套设计是「结构性进步」

1. 传统文件系统：**物理位置 = 逻辑身份**（强耦合，僵化）

2. 你的设计：**物理仅存数据，所有逻辑全部外挂属性**（解耦，无限可扩展）

标签、依赖、状态、分数、生命周期，

全部是**附加结构化信息**，

不再被文件夹树形结构锁死。

这是现代知识管理、工程管理的统一演进方向：

Notion / Obsidian / Docker / Nix 全部是「**存储与逻辑解耦**」范式。

