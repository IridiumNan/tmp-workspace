### 整体设计原则先对齐

你的初始目录结构完全符合 Go 社区的标准 CLI 项目规范，我们在此基础上做细化，全程遵循**极简、不过度设计、职责单一、依赖单向**的原则，和项目「拒绝冗余抽象」的设计哲学保持一致：

1. **依赖方向唯一**：`cmd` → `command` → `store/config/display` → `model`，底层包绝不依赖上层，避免循环依赖
2. **业务内聚**：所有磁盘操作、生命周期逻辑全部收敛在 `store` 层，命令层只做参数解析、流程调度、输出展示
3. **MVP 优先**：先落地核心闭环，不提前做抽象、接口、冗余封装，能用就好，后续迭代再优化

---

## 完整目录结构与文件职责

```
.
├── bin/                    # 编译产物目录，.gitignore 忽略
├── cmd/
│   └── tmp-workspace/      # 二进制入口，目录名和最终二进制名一致
│       └── main.go         # 极简入口，只做初始化、调用命令层、错误退出
├── internal/
│   ├── model/              # 最底层：纯数据结构定义，无业务逻辑，所有包都可引用
│   │   └── workspace.go    # 工作区结构体、状态枚举、ID生成方法
│   │
│   ├── config/             # 配置加载：读取 TOML、处理 XDG 路径、默认值兜底
│   │   └── config.go
│   │
│   ├── store/              # 核心业务层：所有持久化、生命周期逻辑都在这里
│   │   ├── metadata.go     # 元数据读写（原子写入）、工作区增删改查
│   │   ├── workspace.go    # 工作区目录操作、软链接管理、保存/删除物理操作
│   │   ├── cleanup.go      # 自动清理逻辑、过期判断、死链清理
│   │   └── event_log.go    # 生命周期事件日志写入
│   │
│   ├── display/            # 展示层：终端输出格式化、颜色、表格对齐，纯展示无业务
│   │   ├── color.go        # 颜色封装（ANSI 原生，零依赖）
│   │   ├── workspace.go    # 工作区列表/详情格式化输出
│   │   └── log.go          # 日志条目格式化输出
│   │
│   └── command/            # 命令层：子命令定义、参数解析、流程调度
│       ├── root.go         # 根命令注册、全局参数、懒清理前置钩子
│       ├── new.go          # new 子命令
│       ├── ls.go           # ls 子命令
│       ├── save.go         # save 子命令
│       ├── del.go          # del 子命令
│       ├── clean.go        # clean 子命令
│       ├── cd.go           # cd 子命令（P1 迭代）
│       ├── log.go          # log 子命令（P1 迭代）
│       └── config.go       # config 子命令（P1 迭代）
│
├── .gitignore
├── go.mod
├── go.sum
├── config.example.toml     # 配置示例，用户参考用，不是运行必须
├── idea.md
├── plan.md
└── README.md               # MVP 跑通后再补就行
```

---

## 各层核心设计细节

### 1. model 层（最底层，绝对稳定）

只放数据结构和纯工具方法，**不依赖任何其他内部包**，所有层都可以引用。
`model/workspace.go` 核心内容：

- 工作区状态枚举：`StatusPending` / `StatusSaved` / `StatusDeleted`
- `Workspace` 结构体：ID、Name、CreateTime、UpdateTime、Status、Path、SavedPath
- ID 生成函数：`GenerateID()` 生成 12 位小写十六进制随机串
- 状态合法性校验等纯函数方法

### 2. config 层

只做一件事：加载配置，处理路径，提供默认值。
`config/config.go` 核心能力：

- 定义和 TOML 一一对应的结构体（全局配置 + Cleanup 分段 + LinkedDirs 分段）
- `Load()` 方法：优先读用户配置文件，不存在则返回内置默认值，零配置开箱即用
- 自动处理 XDG 环境变量：`XDG_CONFIG_HOME` / `XDG_STATE_HOME` / `XDG_DATA_HOME`
- 自动展开路径中的 `~`，统一返回绝对路径

> 💡 设计要点：配置是只读的，加载后全程不修改，避免状态混乱。

### 3. store 层（核心业务心脏）

所有和磁盘、持久化、生命周期相关的逻辑全部收敛在这里，上层命令层**绝对不直接操作文件**。

- `metadata.go`：元数据的加载、保存（原子写入：临时文件 + rename 替换）、工作区的增删改查方法（Get / List / Add / Update / Delete）
- `workspace.go`：
  - 物理目录操作：创建工作区目录（`by-id/xxx`）、删除目录
  - 软链接管理：创建/删除 `by-name` 别名、清理死链
  - 保存逻辑：同分区直接 `mv`、跨分区降级复制、可选硬链接模式
  - 工具方法：ID 前缀匹配、名称模糊匹配、检查工作区是否存在
- `cleanup.go`：
  - 过期判断逻辑：按 `update_time` + 保留时长 + 宽限期计算
  - 清理执行：跳过当前活跃工作区、删除目录、更新状态、写日志
  - 懒清理触发判断：距离上次清理时间是否超过间隔
- `event_log.go`：统一封装事件日志写入，追加模式打开，保证单条原子性

> 💡 设计要点：这层是唯一会碰磁盘的地方，所有错误、边界情况（比如权限不足、文件不存在）都在这里处理，上层只拿结果。

### 4. display 层

纯展示，零业务逻辑，只负责把 `model` 里的数据转成终端友好的输出。

- `color.go`：封装红/绿/黄/灰等基础颜色函数，原生 ANSI 实现，零第三方依赖
- `workspace.go`：格式化工作区列表，列对齐、状态上色、剩余时间人性化显示（比如 `3天2小时` 而非纯秒数）
- `log.go`：格式化日志条目，按事件类型上色

> 💡 设计要点：如果以后要改输出样式、加 JSON 输出模式，只改这一层，不影响任何业务逻辑。

### 5. command 层

命令的调度层，负责参数解析、调用 store 方法、处理用户交互、调用 display 输出。

- `root.go`：
  - 注册所有子命令
  - 全局前置钩子：判断当前命令是否在 `trigger_commands` 列表里，是则执行懒清理检查
  - 全局错误处理：统一打印错误、退出码
- 每个子命令一个文件：比如 `new.go` 只处理 `new` 命令的参数解析、调用 store 创建工作区、打印结果

> 💡 MVP 阶段直接用标准库 `flag` 实现子命令就行，不用上 `cobra` 这种重型框架，等功能多了再换也完全来得及，符合极简原则。

### 6. cmd 入口

`main.go` 极其薄，只做 3 件事：

1. 加载配置
2. 初始化 store
3. 执行根命令

不要在 main 里写任何业务逻辑。

---

## 推荐开发顺序（MVP 优先）

按照从底层到上层、每一步都可验证的节奏来，避免一次性写太多代码出问题：

1. **第一步：基础底座**
    写 `model/workspace.go` → `config/config.go`，把数据结构和配置加载跑通，写个简单的测试验证配置读取正常。
2. **第二步：持久化核心**
    写 `store/metadata.go` → `store/workspace.go` → `store/event_log.go`，验证元数据读写、工作区创建删除、软链接、日志写入都正常。
3. **第三步：核心业务逻辑**
    写 `store/cleanup.go`，把自动清理逻辑跑通，验证过期判断、清理执行正常。
4. **第四步：展示层**
    写 `display/color.go` → `display/workspace.go`，把列表输出调好看。
5. **第五步：命令层**
    写 `command/root.go` → 核心 5 个命令（`new`/`ls`/`save`/`del`/`clean`）。
6. **第六步：入口串起来**
    写 `cmd/main.go`，编译测试全流程。

MVP 跑通后，再迭代 `cd`、`log`、`config`、`judge` 等 P1 功能，节奏非常稳。

---

## 几个关键设计提醒（贴合之前的共识）

1. **原子写入必须做**：元数据保存必须走「写临时文件 → os.Rename 原子替换」的流程，防止进程中断导致文件损坏，几行代码换极高的可靠性。
2. **日志用追加模式**：打开文件时加 `os.O_APPEND|os.O_CREATE|os.O_WRONLY`，单条日志小于 PIPE_BUF，内核保证写入原子性，不会出现两行穿插。
3. **不要提前抽象**：MVP 阶段不要给 store 写 interface、不要搞依赖注入、不要写单元测试，先让功能跑起来。等以后要换存储、要做测试的时候再重构也完全不迟，现在做就是过度设计。
4. **错误处理直白点**：小工具不用搞复杂的错误包装，直接返回原始错误 + 上下文说明就行，用户能看懂问题在哪。

这个结构的好处是：清晰、好维护、符合 Go 社区惯例，既不会太简陋导致后续扩展乱，也不会过度设计增加开发成本，和你整个项目的「极简务实」调性完全匹配。
