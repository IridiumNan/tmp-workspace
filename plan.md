### 二、plan.md（开发落地计划）

# tmp-workspace 开发计划

## 一、项目概述

tmp-workspace 是面向开发者的临时工作区生命周期管理工具，基于 Linux XDG 目录规范设计，采用懒触发清理机制，核心目标是降低临时创造的心智负担，实现「创建无摩擦、运行不分心、决策不仓促、了结无残留」。

本计划遵循 MVP 原则，优先落地核心闭环，逐步迭代增强功能。

## 二、基础设计约定

### 1. 工作区 ID 规则

- 完整ID：12 位小写十六进制随机字符串，全局唯一，碰撞概率可忽略
- 匹配规则：所有命令支持 ID 前缀模糊匹配（对齐 Docker 使用体验），至少输入 2 位即可定位；匹配到多个结果时列出候选项
- 定位优先级：优先匹配 ID 前缀，无匹配时再尝试匹配工作区名称

### 2. 工作区目录分层

采用「真实存储 + 可读别名」双层结构，基于原生软链接实现：

- `by-id/`：真实工作区目录，以完整 ID 命名，全局唯一，是数据的唯一真实来源
- `by-name/`：名称软链接目录，仅包含用户自定义了名称的工作区，软链接指向 `by-id` 下对应目录，方便手动浏览

### 3. 生命周期状态机

```
pending（待审判）
    ├─ 主动 save → saved（已保存）
    ├─ 主动 del → deleted（已删除）
    └─ 超时自动清理 → deleted（已过期清理）
```

- 所有状态变更均写入事件日志，永久留痕
- saved / deleted 状态仅保留元数据记录，不占用磁盘空间

## 三、核心命令集

### 1. 工作区操作类

#### `new` [自定义名称]

- **功能**：创建一个全新的临时工作区
- **逻辑细节**：
  1. 生成 12 位十六进制唯一 ID，在 `by-id` 目录下创建对应工作区
  2. 若传入自定义名称，则在 `by-name` 目录下创建同名软链接；名称强制唯一，重名时提示用户更换
  3. 若 `linked_dirs.auto_create = true`，自动将配置的常用目录软链接到工作区根目录
  4. 写入元数据，标记状态为 `pending`，记录创建时间与更新时间
  5. 输出工作区绝对路径，配合 Shell 包装可自动进入目录
- **日志事件**：`CREATE`
- **清理触发**：是

#### `cd` <工作区ID/名称>

- **功能**：进入指定的已有工作区
- **逻辑细节**：
  1. 支持 ID 前缀匹配、名称模糊匹配；匹配到多个时列出结果供选择
  2. 更新工作区的 `update_time`，刷新过期倒计时
  3. 设置环境变量 `TMP_WORKSPACE_ID` 标记当前活跃工作区，用于防误删
  4. 输出工作区绝对路径，配合 Shell 包装自动切换目录
- **清理触发**：否

#### `del` / `delete` <工作区ID/名称...>

- **功能**：手动删除指定工作区，支持批量
- **逻辑细节**：
  1. 默认删除前二次确认，加 `-f` / `--force` 参数可跳过确认
  2. 若工作区有自定义名称，先删除 `by-name` 下对应的软链接
  3. 立即删除 `by-id` 下的真实工作区目录
  4. 元数据标记状态为 `deleted`，保留历史记录
- **日志事件**：`MANUAL_DELETE`
- **清理触发**：是

#### `save` [目标路径]

- **功能**：永久保存工作区到指定目录，结束临时生命周期
- **逻辑细节**：
  1. 不传参数时，默认保存到 `archive_dir` 配置的归档目录，以工作区名称命名子目录
  2. 目标路径不存在：直接 `mv` 移动工作区目录，零拷贝瞬时完成
  3. 目标路径已存在：递归创建硬链接，零额外空间开销；跨分区硬链接失败时自动降级为复制
  4. 同步删除 `by-name` 下对应的软链接
  5. 元数据标记状态为 `saved`，记录保存路径，永久保留记录，不再参与自动清理
  6. 输出保存后的目标路径，配合 Shell 包装可自动进入归档后的目录
- **日志事件**：`ARCHIVE`
- **清理触发**：是

#### `clean`

- **功能**：手动触发全量过期清理，无视时间间隔
- **逻辑细节**：
  1. 遍历所有 `pending` 状态的工作区，清理所有超期项
  2. 输出清理统计：清理数量、释放空间
  3. 更新元数据中的 `last_clean_time`
  4. 顺便清理 `by-name` 目录下的无效死链
- **清理触发**：是

### 2. 查看查询类

#### `ls`

- **功能**：查看工作区列表
- **参数**：
  - 默认：仅显示 `pending` 状态的工作区
  - `-a` / `--all`：显示所有状态（pending / saved / deleted）
- **输出字段**：ID、名称、创建时间、剩余有效期、状态
- **清理触发**：否

#### `log`

- **功能**：查看生命周期事件日志
- **参数**：
  - 默认：倒序显示最近 20 条记录
  - `-n <数量>`：指定显示条数
  - `--type <事件类型>`：按事件类型过滤（create / archive / manual_delete / expire_clean）
- **清理触发**：否

### 3. 系统配置类

#### `config`

- **功能**：快速打开配置文件
- **逻辑细节**：
  1. 优先读取 `$EDITOR` 环境变量启动编辑器
  2. 未设置时依次尝试 `nano` → `vim` → `vi`，均不可用则提示手动打开路径
  3. 首次运行无配置文件时，自动生成带完整注释的默认配置后再打开
- **清理触发**：否

## 四、核心运行机制

### 1. 懒触发式自动清理

#### 核心规则

- 不常驻后台进程、不做定时轮询，仅在用户执行指定命令时被动触发检查
- 只有命令在 `cleanup.trigger_commands` 列表中，且距离上次清理超过 `check_interval` 时，才执行实际清理
- 查看类命令默认不触发清理，保证用户决策时不受干扰

#### 执行流程

```
命令入口拦截
├─ 检查 auto_clean 是否开启 → 关闭则跳过
├─ 检查当前命令是否在 trigger_commands 中 → 不在则跳过
├─ 读取 last_clean_time 计算时间差 → 小于间隔则跳过
└─ 执行清理逻辑
    ├─ 遍历 pending 状态工作区
    ├─ 跳过当前活跃工作区（匹配 TMP_WORKSPACE_ID）
    ├─ 判断 update_time 是否超过 retention_hours + 宽限期
    ├─ 超期工作区：删除目录 → 清理软链接 → 标记 deleted → 写入日志
    ├─ 清理 by-name 下的无效死链
    └─ 更新 last_clean_time
```

### 2. 防误删保护机制

1. **活跃工作区跳过**：清理时自动跳过环境变量标记的当前活跃工作区
2. **更新时间刷新**：每次进入、修改工作区时自动刷新 update_time，正在使用的工作区不会过期
3. **宽限期缓冲**：超过保留时长后，额外增加 12 小时宽限期，避免临界点误删
4. **日志可追溯**：所有删除操作均有明确时间与原因记录，可溯源

### 3. 原子化写入保障

所有元数据、日志写入均采用原子操作，避免进程中断、断电导致文件损坏：

- 元数据：先写入临时文件，写入完成后用 `os.replace` 原子替换原文件
- 日志：追加模式写入，单条日志长度远小于系统 `PIPE_BUF`，内核保证写入原子性

### 4. 软链接别名机制

- 工具内部所有操作均基于 by-id 的真实路径，不依赖软链接
- 软链接仅作为用户手动浏览的便捷入口，损坏、丢失不影响任何核心功能
- 支持通过元数据一键重建所有软链接，容错性极强

## 五、目录规范（遵循 XDG 标准）

所有路径默认遵循 Linux XDG Base Directory 规范，支持通过环境变量全局调整，也支持在配置文件中单独覆盖。

| 路径用途 | 默认位置 | 环境变量 |
|----------|----------|----------|
| 配置文件 | `~/.config/tmp-workspace/config.toml` | `XDG_CONFIG_HOME` |
| 临时工作区根目录 | `~/.local/state/tmp-workspace/workspaces/` | `XDG_STATE_HOME` |
| 元数据文件 | `~/.local/state/tmp-workspace/metadata.json` | `XDG_STATE_HOME` |
| 事件日志 | `~/.local/state/tmp-workspace/events.log` | `XDG_STATE_HOME` |
| 默认归档目录 | `~/.local/share/tmp-workspace/archived/` | `XDG_DATA_HOME` |
| 缓存目录（预留） | `~/.cache/tmp-workspace/` | `XDG_CACHE_HOME` |

### 工作区目录结构

```
workspaces/
├── by-id/
│   ├── a1b2c3d4e5f6/    # 有自定义名称的工作区
│   └── 7890abcdef12/    # 未命名的临时工作区
└── by-name/
    └── 测试Python库demo -> ../by-id/a1b2c3d4e5f6
```

## 六、配置文件规范

配置文件采用 TOML 格式，支持注释，结构清晰，所有字段均有默认值，开箱即用。

```toml
# tmp-workspace 全局配置
# 默认遵循 XDG 目录规范，所有路径均可自定义覆盖

# ========== 核心路径配置 ==========
# 临时工作区根目录
workspace_dir = "~/.local/state/tmp-workspace/workspaces"

# 元数据文件路径
metadata_path = "~/.local/state/tmp-workspace/metadata.json"

# 生命周期事件日志路径
log_path = "~/.local/state/tmp-workspace/events.log"

# 默认归档目录（save 不带参数时默认保存到此）
archive_dir = "~/.local/share/tmp-workspace/archived"

# ========== 生命周期配置 ==========
# 临时工作区默认保留时长，单位：小时，默认 168 小时（7 天）
retention_hours = 168

# ========== 自动清理配置 ==========
[cleanup]
# 是否开启超时自动清理
auto_clean = true

# 执行以下命令时，会自动触发清理检查（仅间隔达标时才执行）
# 查看类命令默认不触发，保证用户决策时不受后台清理干扰
trigger_commands = ["new", "save", "del", "clean"]

# 两次清理的最小时间间隔，单位：分钟
check_interval = 20

# ========== 快捷链接配置 ==========
# 创建工作区时，自动软链接到工作区内的常用目录
[linked_dirs]
# 是否自动创建软链接
auto_create = true

# 系统临时目录
tmp = "~/tmp"
# 系统下载目录
downloads = "~/Downloads"
```

## 七、元数据与日志结构

### 1. metadata.json 结构

```json
{
  "last_clean_time": 1718341200,
  "version": 1,
  "workspaces": {
    "a1b2c3d4e5f6": {
      "id": "a1b2c3d4e5f6",
      "name": "测试Python库demo",
      "create_time": 1718328000,
      "update_time": 1718335200,
      "status": "pending",
      "path": "~/.local/state/tmp-workspace/workspaces/by-id/a1b2c3d4e5f6",
      "saved_path": null
    }
  }
}
```

- 状态枚举：`pending` / `saved` / `deleted`
- 时间均为 Unix 时间戳，单位：秒

### 2. events.log 日志格式

每行一条记录，固定字段顺序，纯文本格式，直接支持 grep 过滤：

```
[ISO时间戳]  [事件类型]  [工作区ID]  "[工作区名称]"  [补充信息]
```

示例：

```log
2024-06-15T10:00:00+08:00  CREATE         a1b2c3d4e5f6  "测试Python库demo"
2024-06-15T16:30:00+08:00  ARCHIVE        a1b2c3d4e5f6  "测试Python库demo"  归档至: ~/.local/share/tmp-workspace/archived/测试Python库demo
2024-06-22T10:00:00+08:00  EXPIRE_CLEAN   7890abcdef12  "废弃测试脚本"      原因: 超出7天保留期
```

事件类型：`CREATE` / `ARCHIVE` / `MANUAL_DELETE` / `EXPIRE_CLEAN`

## 八、MVP 落地优先级

### P0：核心闭环（必须实现）

1. 基础命令：`new` / `ls` / `save` / `del` / `clean`
2. 核心机制：元数据管理、懒触发清理、生命周期状态机
3. 基础能力：by-id 目录创建、软链接别名、原子写入、硬链接保存
4. 规范落地：XDG 目录适配、TOML 配置解析、事件日志

### P1：体验优化（核心闭环后迭代）

1. `cd` 命令 + Shell 包装脚本（自动切换目录）
2. ID 前缀匹配、名称模糊匹配
3. `log` 命令的过滤与条数参数
4. `config` 命令自动生成默认配置
5. 工作区更新时间自动刷新
6. 懒清理时顺便清理 by-name 死链

### P2：增强功能（远期规划）

1. 工作区重命名、标签分类
2. 批量保存、批量清理
3. 日志自动轮转与过期清理
4. 元数据损坏自动重建、软链接一键修复
5. 多设备同步能力

```
